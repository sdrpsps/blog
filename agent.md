# 极简主义设计与开发规范指南 (Minimalism Design & Coding Guide)

本博客项目已全面重构至 Astro，并遵循极简主义设计规范。在后续的修改、功能添加和代码维护中，请务必严格遵守以下原则：

## 1. 视觉设计原则

- **极致留白**: 保持充足的内边距和外边距。段落之间、文章列表项之间应有自然的呼吸感，尽量避免拥挤。
- **色彩克制**: 
  - 浅色模式下，以 `#ffffff` 为底色，`#111111` 为主要文字颜色，`#666666` 为辅助文字。
  - 深色模式下，以 `#0a0a0a` 为底色，`#e5e5e5` 为主要文字颜色，`#888888` 为辅助文字。
  - 强调色（Accent Color）在极少数需要引起注意的交互元素（如链接、高亮状态）上使用，且应使用系统默认的高对比色，不可大面积铺设。
- **去除冗余装饰**: 
  - 不使用任何复杂的卡片阴影（如 `shadow-lg`）、多层背景过渡、花哨的拟物化或渐变色块。
  - 仅在必要的分区处使用厚度为 `1px`、透明度极低的细边框或分割线（如 `rgba(0, 0, 0, 0.05)` 或 `rgba(255, 255, 255, 0.05)`）。
- **优秀的排版**:
  - 中文字体统一采用「霞鹜文楷 Lite (`LXGW WenKai Lite`)」。
  - 正文字体大小保持在 `16px - 18px` (`1rem` - `1.125rem`)，行高设置为 `1.75 - 1.8`，段落底边距为 `1.5rem`。

## 2. 交互与动画规范

- **原生滚动**: 移除 Lenis 等重型的平滑滚动 JS 库。在 CSS 中使用 `html { scroll-behavior: smooth; }` 实现自然的锚点平滑滚动。
- **超快反馈**: 所有 hover 和 active 交互的过渡效果（Transition）必须极其微妙、平滑。过渡时间控制在 `150ms` 左右，曲线使用 `cubic-bezier(0.4, 0, 0.2, 1)`。
- **无感加载**: 主题切换时，应有温和的渐变效果，而不是生硬的闪烁。

## 3. 性能与架构约束 (Astro Islands)

- **零客户端 JS (Zero-JS by Default)**: 首页、文章列表页等无交互页面必须 is 100% 静态 HTML，不得引入客户端 JavaScript 或 Hydrate 任何 React 组件。
- **局部水合 (Partial Hydration)**: 只有真正需要复杂交互的组件（如 `MDXMusicCard` 音乐卡片）可以使用 React 并声明 `client:visible`。
- **CSS 优化**: 所有的全局样式和主题变量使用原生 CSS 变量维护，Tailwind 用于快速排版，但要保持构建后的 CSS 极其轻量。
- **Giscus 评论**: 移除 `@giscus/react` 包，直接通过原生 Web Component (`<giscus-widget>`) 加载评论，确保不在首屏阻塞页面加载。
