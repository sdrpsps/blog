# 极简主义设计与开发规范指南 (Minimalism Design & Coding Guide)

本博客项目已全面重构至 Astro，并遵循“纸质低语 (The Whispering Pages)”极简主义设计规范。在后续的修改、功能添加和代码维护中，请务必严格遵守以下原则：

---

## 1. 视觉设计原则

### 🪵 材质与色调 (Paper & Cozy Ink)
*   **浅色模式 (宣纸温润)**：以温润柔和的宣纸色 `#faf8f5` 为底色，`#1e1e1d` 为主要文字（深墨）颜色，`#787870` 为辅助文字（烟灰）颜色。
*   **深色模式 (碳墨夜空)**：以碳墨色 `#0d0d0d` 为底色，`#e2e2e0` 为主要文字颜色，`#888882` 为辅助文字颜色。
*   **沉稳黛绿强调色**：强调色（Accent Color）在极少数需要引起注意的交互元素（如当前激活的目录项）上使用具有文人气质的黛绿（浅色下 `#3f5246`，深色下 `#788a7e`），不可大面积铺设。

### 📐 书籍装帧版心与辅助线 (Editorial Layout & Grid Lines)
*   **版心辅助线**：在大屏幕（`lg` / `xl`）上，主体内容（Header, Main, Footer）的左右两侧必须绘制 `0.5px` 的垂直细实线 (`var(--border)`) 构成版心边界，且内部留有 `3rem` 的内边距（padding），模拟实体书籍页面的页边装帧线。
*   **禁止冗余命名**：Header 已经包含站点 Logo/名称，首屏 Hero 区域禁止再次使用巨大的站点名称标题。应改为富有文学色彩的 **“扉页序言 (Preface)”** 版式：配有 `VOL. 01 / PREFACE` 极细徽章，展示具有温度的短句。

### 📰 非对称年鉴时间轴与左文右图 (Almanac Timeline & Asymmetric Editorial)
*   **纤细日期**：列表日期的字间距极宽（例如 `2024 . 10 . 01`），且字号极小、字重极轻，作为独立一栏放置在左侧。
*   **左文右图编排**：改变笨重的“大图顶天立地”结构，采用**文字为主、图片为辅**的非对称布局。
    *   **文字**：标题在顶，紧跟摘要，最下方显示大写字数标签（如 `WORDS`），保持高度可读性。
    *   **缩略图画框**：图片缩小为右侧的精致缩略图（`4:3` 比例），外围包裹 `0.5px` 细线与内嵌底色画框。图片默认带有 `15%` 的胶片感 `sepia` 滤镜，Hover 时缓慢褪去并轻微放大。

---

## 2. 交互与动画规范

- **纯 CSS 聚焦交互 (Focus Effect)**:
  在不使用任何客户端 JS 的前提下，利用 CSS 选择器实现文章项的悬浮聚焦：
  当鼠标悬停在列表上时，当前 hover 的文章项微微向右偏移 `6px`，而其余所有同辈文章的透明度微妙降低至 `0.45`，创造宁静的“聚焦阅读”体验。
- **主题切换 3D 渐变**:
  切换主题时禁止生硬的闪烁。使用 absolute 定位将多状态 Icon 重合，在切换时加入 `500ms` 的 3D 旋转缩放过渡动画（90度旋转 + 缩小至 0.5 + 淡入淡出），实现微风拂面般的柔和过渡。
- **动态 TOC 刻度指示线**:
  右侧目录 (TOC) 的每个链接左侧均应设有一个微小的圆点刻度 (`.toc-marker`)。当页面滚动到对应章节时，刻度线在 `300ms` 过渡动画下“长高”为一条黛绿色的竖向进度线，其他刻度线收缩为半透明圆点。
- **原生滚动**: 移除 Lenis 等重型的平滑滚动 JS 库。在 CSS 中使用 `html { scroll-behavior: smooth; }` 实现自然的锚点平滑滚动。
- **超快反馈**: 所有 hover 和 active 交互的过渡效果（Transition）必须极其微妙、平滑。过渡时间控制在 `150ms` 左右，曲线使用 `cubic-bezier(0.4, 0, 0.2, 1)`。

---

## 3. 性能与架构约束 (Astro Islands)

- **零客户端 JS (Zero-JS by Default)**: 首页、文章列表页等无交互页面必须是 100% 静态 HTML，不得引入客户端 JavaScript 或 Hydrate 任何 React 组件。
- **局部水合 (Partial Hydration)**: 只有真正需要复杂交互的组件（如 `MDXMusicCard` 音乐卡片）可以使用 React 并声明 `client:visible`。
- **CSS 优化**: 所有的全局样式和主题变量使用原生 CSS 变量维护，Tailwind 用于快速排版，但要保持构建后的 CSS 极其轻量。
- **Giscus 评论**: 移除 `@giscus/react` 包，直接通过原生 Web Component (`<giscus-widget>`) 加载评论，确保不在首屏阻塞页面加载。
